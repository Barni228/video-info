// Root of the UI. build.rs compiles this file, so everything Rust needs to
// reach -- both windows, the InfoRow struct, the Theme global -- has to be
// exported from here.
export { ThemeMode, Theme } from "theme.slint";
export { AppWindow, InfoRow } from "main-window.slint";
export { SettingsWindow } from "settings-window.slint";
